{
  "name": "invalid-battery-level-type",
  "description": "$battery_level is a typed device-health property and must be a number. A string value rejects the event with invalid_property_value. As the only event here, nothing is left to store, so the whole request is a 422. ($wifi_rssi follows the same rule.)",
  "wire_format": "honch-json-v1",
  "request": {
    "method": "POST",
    "path": "/capture",
    "headers": {
      "Content-Type": "application/json",
      "X-Honch-Project-Key": "<project_api_key>"
    },
    "body": {
      "context": {
        "distinct_id": "user-1",
        "$device_id": "device-1",
        "$device_model": "model-x",
        "$firmware_version": "1.0.0",
        "$sdk_platform": "posix",
        "$sdk_version": "0.2.3"
      },
      "events": [
        { "event": "$battery_low", "properties": { "$battery_level": "low" } }
      ]
    }
  },
  "expected_ingest_status": 422,
  "expected_error_codes": ["invalid_property_value"]
}
